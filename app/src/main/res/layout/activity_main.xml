<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.vadim.myapplication.MainActivity"
    android:background="?attr/colorButtonNormal">

    <Button
        android:text="Авторизоваться"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="153dp"
        android:id="@+id/btnAuth"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:onClick="TryAuthorize"
        android:background="@android:drawable/editbox_dropdown_light_frame" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="textPassword"
        android:ems="10"
        android:id="@+id/editWorkerPasswd"
        android:background="@android:color/background_light"
        android:layout_above="@+id/btnAuth"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="44dp"
        android:hint="Пароль" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="number"
        android:ems="10"
        android:id="@+id/editWorkerId"
        android:background="@android:color/background_light"
        android:layout_marginBottom="18dp"
        android:layout_above="@+id/editWorkerPasswd"
        android:layout_alignLeft="@+id/editWorkerPasswd"
        android:layout_alignStart="@+id/editWorkerPasswd"
        android:hint="ID" />

</RelativeLayout>
